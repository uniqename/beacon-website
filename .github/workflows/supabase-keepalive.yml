name: Supabase Keep-Alive

on:
  schedule:
    # Runs every 3 days at 08:00 UTC — well within Supabase free-tier 7-day pause window
    - cron: '0 8 */3 * *'
  workflow_dispatch: # allow manual trigger from GitHub Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HomeLinkGH
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://leuizklnwukrseocweyu.supabase.co/rest/v1/" \
            -H "apikey: sb_publishable_afnYXMXEBFVH0AroXDZFcQ_qBYB0EAy")
          echo "HomeLinkGH → $STATUS"

      - name: Ping Beacon
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://mloatbnakkjbmsrvzybk.supabase.co/rest/v1/" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1sb2F0Ym5ha2tqYm1zcnZ6eWJrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM0MTU2NzUsImV4cCI6MjA4ODk5MTY3NX0.DJAhPOvItx381PRpC61xKZ4t7UG5UVHgyRNrWKJfrdM")
          echo "Beacon → $STATUS"

      - name: Ping FaithKlinik
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://pfitpxihsynuidibzyqa.supabase.co/rest/v1/" \
            -H "apikey: sb_publishable_CiPWVmT3-xqTluGSaf6yig_kCuSfJvC")
          echo "FaithKlinik → $STATUS"

      - name: Ping Ekjona
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://zsbjvurxorrnxnnotmbq.supabase.co/rest/v1/" \
            -H "apikey: sb_publishable_5gm3VrXrdwtBwTme1JOU3Q_v7LH-E5N")
          echo "Ekjona → $STATUS"

      - name: Summary
        run: echo "All 4 Supabase projects pinged. Any 200/400 response counts as activity."
